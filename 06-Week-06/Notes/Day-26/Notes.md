</Markdown>

# Day 26 Notes

# Concepts Learned
- wsl2
- linux filesystem
- linux filesystem navigation
- linux files creation
- bash to automate creation of files and directories

# Key Commands
- ls, ls -la, pwd, uname -a, cd ~, cd -, echo, if (()) and inf [[]] in conjuction with testing for numbers and strings respectively, use if if elif fi, for loop and done, positional parameters, use of wildcards in if, mkdir -p for idempotent directory creation

# Key Observations
- bash is a very rich programming language, it is a much more improved scripting language than the old-school sh system (bourne shell).  

# Lessons Learned
- programming in bash is similar to programming in python in that bash works directly with system utilities and functions available in the operating system


# Questions For Future Study
- prepare for Day 27

