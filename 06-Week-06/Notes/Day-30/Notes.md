</Markdown>

# Day 30 Notes

# Concepts Learned
- used system utilities from within python to run system commands
- used importlib to verify packages to be installed 
  and dependencies being able to be imported successfully

# Key Commands
sys.platform, sys.executable, sys.prefix, os.environ, sys.base_prefix, os.getenv, importlib.import_module, version(), subprocess()

# Key Observations
- subprocess() returns a code, which can be used to determine
- who a program should run

# Lessons Learned
- bash and python can peform similar functions by the use of system, os, sub_process(), platform, shutil,

# Questions For Future Study
- study object-oriented design for python

