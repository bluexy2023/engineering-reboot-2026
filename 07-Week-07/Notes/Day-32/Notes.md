</Markdown>

# Day 32 Notes

# Concepts Learned
- encapsulation and getter functions
- use of private attributes by prepending with _ a variable name
  inside the class
- use of protected attributes which is not accessible outside of the class by prepending with __ a variable name

# Key Commands
- same python commands but just about classes and methods using 
  private and protected variables

# Key Observations
- I need to bear in mind that trailing commas in definitions for functions, tuple, sets, dictionaries, lists is a good practice as this allows for 1 line less of a change when a these definitions are added or extended with another entry at the end (diff uses this)

# Lessons Learned
- objects, variables, PEP coding style, trailing commas after definitions for the last parementer or element

# Questions For Future Study
-none

