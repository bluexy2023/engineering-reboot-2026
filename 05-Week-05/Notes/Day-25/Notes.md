</Markdown>

# Day 25 Notes

# Concepts Learned
- persistence using JSON
- writing python objects to file using serialization
- read/deserialyzing data from file into python objects
- updating stored data and let them persist
- seperate business logic from data

# Key Commands
- json.dump()
- json.load()
- next(), to get a single result from a search
- filtering using list comprehension using a predicate
- sorting using sorted using key lambda functions
- max() value of a list using key lambda functions
- min() value of a list using key lambda functions
- formatted printing
- validated inputs and dispatch tables using descriptions and handlers
- print, for(), while, is, not, None, dict, list, callable, break, continue, if, elif, else, "in" command, "with", open - for opening files, slices .. :n of a list, try, except, raise, ValueError, finally, list operations, set operations, list.remove(), del(), items(), keys(), pop()

# Key Observations
- this day and the day before exercises allowed me to type independently and code independently as opposed to just type the suggested code snippet.  While I missed that, the new behavior of VSCode allowed me to think more thoroughly about the implementation.
- separation of data and business logic is a process of isolating data processing from the data itself. in code, it manifests as a function call to retrieve the data, and process it in the business logic code routine
- also, creation of generic reusable function mimics the way industry software developers write code. it allows for the creation of cleaner and leaner code

# Lessons Learned
- data persistence is the act of processing data that can be stored in a persistent storage to access them in between program execution.  some methods to implement this is through json files, database, or csv files.

# Questions For Future Study
I'll have questions along the way

