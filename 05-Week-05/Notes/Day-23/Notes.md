</Markdown>

# Day 23 Notes

# Concepts Learned
- list manipulation
- set manipulation
- dictinary manipulation
  

# Key Commands
list.append(), list.insert() at location, list.pop(index), list.remove(by value), max(list), sum(list)
set.add(), set.remove(value), len(set)
dictionary.items(), dictionary (add new key), dictionary, assign new value, dictionary remove key using 'del'

# Key Observations
use of max(), sorted(), min(), built-in functions is more efficient than creating own routines, more pythonic code leads to more readable code.  use of lamba functions to return keys for sorting, identifying max or min from an iterable is an efficient approach to coding in python

# Lessons Learned
learned the difference between list comprehension and use of internal functions in conjunction with keys and lamba functions.

# Questions For Future Study
how to organize directory structure to be able to reference common data from other directories

