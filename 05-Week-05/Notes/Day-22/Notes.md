</Markdown>

# Day 22 Notes

# Concepts Learned
- learned about input validation alongside handline exceptions for inputs to illustrate isolating validation from the business logic


# Key Commands
for in range(), input(), try except, while True, break, return, def, print(f""),

# Key Observations
creating an infinite loop to continually prompt for a correct and validated user input is a good way to sanitize user input in console-based applications

# Lessons Learned
learned that a set is a hash-table, that when printed, is not ordered the way it was defined.  the best way to display it in an ordered fashion is to use the "sorted"() function to sort it for display
- input validation is the process of validating values based on its contraints (i.e ranges, length, size, weight, etc), while exception handling is the handling of inputs based on the required data types
- input sanitation is the process of trimming inputs from leading and trailing spaces, as well as eliminating bad inputs from propating into the system by failing fast, and exiting quickly

# Questions For Future Study
what's next ?
