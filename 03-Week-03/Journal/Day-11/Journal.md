# Day 11 Journal

# Summary
- lerned and reviewed on using lists in python programs to implement
  a collection of similar objects or items
# Challenges Encountered
-- the use of the index '-1' to access the last item on the list seems counter-intuitive
# Solutions Applied
-- I interpreted the negative number as the number that you subtract to the lists' length to access a certain item
Ex in: numbers = [10,20,30,40,50], numbers[-2] would give you the item at position list length - 2, which is numbers[5-2], which is numbers[3], which is 40
# Confidence Assessment
9/10
# Reflection
- list is a convenient way of storing values of similar 'types', or a collection of similar items.
-- it differs with storing values in separate variables in that lists
   allows for a collection, or an array of items to be stored in a single variable; access to which could be done through the use of indeces
# Tomorrow Preparation
-- prepare for Collection Traversal and Iteration