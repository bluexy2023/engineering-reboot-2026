## Day 12 Journal

# Summary
performed more exerises on lists and performing operations on them. 
# Challenges Encountered
formating the grades display in one line with commas in between grades from within the for loop
# Solutions Applied
used string concatenation in conjunction with a test (If total grades is not zero, meaning, this is not the first grade in the list, which would have a common string format)
# Confidence Assessment
9/10
# Reflection
processing a collection is more powerful than processing individual variables because it allows for simplifying variable names into relatable names of collection of objects.  Not only does it make programs readable, it also allows for more efficient use of computer power in that it doesn't have to allocate memory for various variable names.
# Tomorrow Preparation
prepare for dictionaries and structured data