</Markdowm>

# Day 08 Notes
## Concepts Learned
- looping 
- use of shorthand for accumulator
## Key Commands
- for internal python command to implement looping
- range() to generate a range of values depending on paramenter -- ex. range(x,y) <- gives a range of values from x until y minues 1
- += as a shorthand for addition to the same variable (accumulator)
## Key Observations
- the for loop requires a temporary variable while it iterates through values IN a list (ex. generated by list, or eventually from other sources)
- also, the for loop can take a third paramter, which dictates the steps or gap in the series of numbers it generates. Ex. range(1,11,2) -- it will generate even numbers from 1 until 10 (i.e, 2,4,6,8,10) 
## Lessons Learned
the for loop and the range() commands are a good combiation of commands to implement counting, and perhaps later on generate histograms and patterns
## Questions For Future Study
None for today