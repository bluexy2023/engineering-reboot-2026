</Markdown>

# Day xx Notes

# Concepts Learned
- opening a file for writing
- writing to a file
- opening a file for reading
- reading a file
- use of with statement along with open("filename", "action") -- action being either, "r" - read, "w" - write, "a" - append
- reading lines by just using the file descriptor object by itself
# Key Commands
with, open, read(), while, if, elif, else, def 
True, break, for line in file
# Key Observations
I have observed that when you iterate from the result of read(), it reads all characters at once, if you want to read lines, you have to use readline()
# Lessons Learned
- files input/output is a persistent way of storing information
# Questions For Future Study
- none so far