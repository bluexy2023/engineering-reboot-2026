</Markdown>

# Day 18 Notes

# Concepts Learned
- reading a CSV file and constructing a collection of records by the use of a list of dictionaries
- performing data analytics on a dataset
# Key Commands
sorted, with open as file, for loop, if, then, list comprehension with condition, aggregation on a list, creation of a sorted list of dictionaries, keys(), items() in a key-value pair list within a single dictionary record. rstrip('character'), split('character') -- first to strip the line with a certain character, for example "\n", strip() on the other hand allows for a line to be chopped into pieces or words depending on what has been defined as the delimiter character. ex. "," for CSV files.
# Key Observations
key-value pairs in dictionaries should explicitly cast variables to int() if expected value is an integer, otherwise, it will just be treated as a string.  list comprehension is a very powerful too to use to summarize certain key components of a data set to isolate and perform aggregation and separate analysis
# Lessons Learned
data processing from a file, iteration, traversion, aggregation, list comprehension, sorted, which is new
# Questions For Future Study
none
