</Markdown>

# Day 20 Notes

# Concepts Learned
- python modules
- importing functions from modules
- separation of business logic from user interface
- reusable modules
# Key Commands
- import, Raise, Except, int(), float(), modulus operator(%), floor division operation (//), exponentiation operator (**), using a touple tuple = (x,y), and unpacking using *tuple, print, while, try, if, elif, else, continue, break
# Key Observations
- reusable modules are like function definitions in C that you can include its header file definition
# Lessons Learned
- modules allow programmers to separate business logic from user inteface, and allows for defining repeatable routines to be isolated for reuse by other software components
# Questions For Future Study
none
