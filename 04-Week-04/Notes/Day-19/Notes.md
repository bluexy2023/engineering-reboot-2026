</Markdown>

# Day 19 Notes

# Concepts Learned
- json fundamentals
- reading jason through json.load(file)
- writing data into a json file through json.dump(data,file)
- traversing through dictionary data through items
# Key Commands
- json.load(), json.dump(),dict.items(), not, None, True
# Key Observations
- json has a more structured way of assembling data into memory, and python sees it the way it was defined in the json file and vice-versa
# Lessons Learned
- json is an essential module to read and populate structured data from and into a file.
# Questions For Future Study
- none
